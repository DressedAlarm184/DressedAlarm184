name: Auto Close Completed Issues

on:
  project_card:
    types: [moved]

jobs:
  check-project-status:
    runs-on: ubuntu-latest
    steps:
      - name: Check if the project card is in a completed column
        if: github.event.project_card.column_name == 'Completed' && github.event.project_card.project_id == 3
        run: |
          echo "The project card is in a completed column."
          # Add your desired actions here
